const rows = [{ id: 'pokpok', data: { name: 'plecy' } }]

export default rows

// this.url = 'localhost://4433'

// this.rows = [{ id: 'kjn', data: { name: 'plecy' } }]
// const url = this.url + '/data.json'
// const response = await fetch(url)
// console.log(response)
